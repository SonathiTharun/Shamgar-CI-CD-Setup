name: CI/CD Pipeline

# Trigger the workflow on push or pull request to main branch
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # (Optional) If you want to push the image to Docker Hub:
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: docker login -u sonathitharun
          password: dckr_pat_A-VoyK0hHxNOwdrUuE8TZV5j9iM

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: false # Set to false if you only want to build the image
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/my-ci-cd-app:latest
